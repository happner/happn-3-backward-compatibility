language: node_js

env:
  - NODE_TLS_REJECT_UNAUTHORIZED=0

os:
  - linux

before_install:
  - if [[ `npm -v` != 3* ]]; then npm i -g npm@3; fi

node_js:
  - "11"
  - "10"
  - "8"

script: "npm test"
